# Development Environment

- Do not install dependencies, build, test, or run this project on the local workstation.
- Use the SSH host `rn` for dependency installation, builds, tests, Docker, and runtime debugging.
- Limit local actions to source editing and Git operations.
